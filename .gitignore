# ---------------------------------------------------------------------------
# Secrets — never commit. Config comes from environment variables only.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_ed25519*
secrets/
credentials.json
service-account*.json

# Token and credential caches. MSAL writes one of these once Microsoft Entra ID
# is introduced; the cloud CLIs write the rest. All contain live credentials.
*.token
*.token.json
token_cache*.json
.msal_cache*
.azure/
.aws/
.secrets/
*.netrc
.netrc

# ---------------------------------------------------------------------------
# Imported client data. Vendor inventory files, catalog extracts and exports are
# commercially sensitive and are business data, not source code. Blocked by
# extension so a file dropped into the repo to "try something" cannot be
# committed by accident; test fixtures are allowed back in explicitly.
# ---------------------------------------------------------------------------
*.csv
*.xlsx
*.xls
*.xlsm
*.pbix
data/
exports/
extracts/
!backend/tests/**/fixtures/**
!backend/tests/fixtures/**
!**/fixtures/**/*.csv
!**/fixtures/**/*.xlsx

# ---------------------------------------------------------------------------
# Retained import files. Raw files are business data, not source code, and may
# contain commercially sensitive vendor pricing. Directories are kept, contents
# are never committed. See docs/decisions/0004-retain-raw-import-files-unchanged.md
# ---------------------------------------------------------------------------
storage/**
!storage/
!storage/raw/
!storage/processed/
!storage/rejected/
!storage/**/.gitkeep

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib60/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
ENV/
env/
.python-version

# Test / coverage / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.nox/
.hypothesis/
junit*.xml

# ---------------------------------------------------------------------------
# Node / Next.js
# ---------------------------------------------------------------------------
node_modules/
.next/
out/
build/
.swc/
*.tsbuildinfo
next-env.d.ts
.eslintcache
.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.turbo/
.vercel/

# ---------------------------------------------------------------------------
# Docker
# ---------------------------------------------------------------------------
docker-compose.override.yml

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ---------------------------------------------------------------------------
# Logs / temp
# ---------------------------------------------------------------------------
*.log
logs/
tmp/
temp/
