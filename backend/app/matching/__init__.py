"""Deterministic product matching (CLAUDE.md §5).

Only :mod:`app.matching.normalize` exists so far — the identifier
normalisation both the Amazon listings mapping and the vendor import share.
The priority-chain engine for vendor rows arrives with phase 7.
"""
