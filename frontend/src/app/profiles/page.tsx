import { Placeholder } from "@/components/Placeholder";

export default function ProfilesPage() {
  return (
    <Placeholder
      title="Import profiles"
      phase="Phase 4"
      summary="Per-vendor, versioned configuration describing how that vendor's file is read. Profiles are data, not code, so onboarding a vendor needs no deployment."
    />
  );
}
