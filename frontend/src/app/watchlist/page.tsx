import { Placeholder } from "@/components/Placeholder";

export default function WatchlistPage() {
  return (
    <Placeholder
      title="Watchlist"
      phase="Phase 9"
      summary="Out-of-stock items being watched, so a vendor restock surfaces immediately."
    />
  );
}
