import { Placeholder } from "@/components/Placeholder";

export default function VendorsPage() {
  return (
    <Placeholder
      title="Vendors"
      phase="Phase 2"
      summary="Supplier records: code, name, status, currency, contact details, and lead-time defaults. Create, edit, and deactivate, with every change audited."
    />
  );
}
