import { Placeholder } from "@/components/Placeholder";

export default function AuditPage() {
  return (
    <Placeholder
      title="Audit log"
      phase="Phase 1"
      summary="Append-only trail of important data changes: actor, action, entity, before and after state, and a UTC timestamp."
    />
  );
}
