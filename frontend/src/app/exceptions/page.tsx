import { Placeholder } from "@/components/Placeholder";

export default function ExceptionsPage() {
  return (
    <Placeholder
      title="Exception queue"
      phase="Phase 8"
      summary="Every uncertain product match lands here for human review. Approving one creates a permanent mapping that matches automatically on later imports."
    />
  );
}
